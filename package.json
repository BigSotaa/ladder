{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "build-css": "tailwindcss -i ./styles/input.css -o ./styles/output.css --build && minify ./styles/output.css > ./cmd/styles.css",
    "start": "node server.js",
    "test": "jest"
  },
  "dependencies": {
    "tailwindcss": "^3.0.0",
    "minify": "^7.0.0"
  },
  "devDependencies": {
    "jest": "^27.0.0"
  }
}